# Server

# Start
Using new --watch flag insteade use nodemon
``node --watch index.js```

Testing server
```curl -N localhost:3000```

-i | Give the headers
-X | Set the method
```curl -i -X OPTIONS localhost:3000```